# Copy this file to .env.local (gitignored) and fill in the value.
# Both Next.js (auto) and the Python pipeline (via pipeline/src/analyze.py)
# read .env.local from this directory.

ANTHROPIC_API_KEY=sk-ant-xxxx

# Optional overrides:
# GOLF_COACH_BACKEND=api   # or "cli" to force the claude -p CLI (Max plan)
